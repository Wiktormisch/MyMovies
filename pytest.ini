[pytest]
DJANGO_SETTINGS_MODULE = MyMovies.settings_test
python_files = test_*.py
testpaths = tests
addopts = -v --strict-markers
markers =
    tmdb: tests covering the TMDB integration (requests is always mocked)
