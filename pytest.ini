[pytest]
DJANGO_SETTINGS_MODULE = MyMovies.settings_test
python_files = test_*.py
testpaths = tests
addopts = -v --strict-markers
markers =
    tmdb: testy dotykające integracji z TMDB (requests jest mockowany)
